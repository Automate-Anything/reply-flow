export default function DebugOverlay() {
  // Element hover inspector disabled — other debug features (AIDebugPanel,
  // KBPipelineVisualizer, etc.) remain active via their own components.
  return null;
}
